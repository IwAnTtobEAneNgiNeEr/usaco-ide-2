# Media

Screenshots embedded by the root `README.md` live here. The PNGs below are captured and
committed; an optional demo GIF can be added later.

| File | Shot |
|------|------|
| `hero.png` | Banner: the full three-column workbench. |
| `editor.png` | Editor + Run/Judge console showing an **AC** verdict + diff. |
| `statement.png` | The rendered problem statement with worked examples. |
| `coach.png` | The AI Coach diagnosing a Wrong Answer in context. |
| `tests.png` | The Tests panel with per-test results. |
| `lab.png` | The 🧪 Stress Lab mid-run (generator + brute, a found mismatch). |
| `visualizer.png` | The 🔬 Visualizer rendering a graph or grid from a test input. |
| `demo.gif` *(optional, not yet captured)* | 10–15s: paste a screenshot → 🔎 Analyze → write code → Judge → **AC**. |

Tips for re-capturing: use a clean window (Zen mode `Alt+Z` hides side panels), the dark
theme, and a real-looking problem. Keep any GIF under ~5 MB so it loads on GitHub.
